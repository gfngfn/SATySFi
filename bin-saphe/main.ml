

let version =
  "Saphe version 0.0.1 alpha"


let solve
    ~fpath_in:(_ : string)
    ~show_full_path:(_ : bool)
    ~config_paths_str_opt:(_ : string option)
    ~no_default_config:(_ : bool)
=
  failwith "TODO: migrate `solve` from `satysfi`"
